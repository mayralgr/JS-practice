// Query selector all
// regresa todos los elementos que cumplan la condición del selector
const card = document.querySelectorAll('.card');
// nodelist
console.log(card);


const formualrio = document.querySelectorAll('#formulario');
console.log(formualrio);

// SI no existe, regresa un nodelist vacio
const no = document.querySelectorAll('.do');
console.log(no);
