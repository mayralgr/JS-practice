let resultado;

// PI

resultado = Math.PI;

// redondear

resultado = Math.round(2.8); // hacia arriba
resultado = Math.round(2.2); // hacia abajo

resultado = Math.round(2.6); // hacia arriba
resultado = Math.round(2.5); // hacia arriba

// redondear hacia arriba

resultado = Math.ceil(2.5); // hacia arriba

// redondear hacia abajo

resultado = Math.floor(2.9); // hacia abajo

// raiz cuadrada

resultado = Math.sqrt(144);

// absoluto
resultado = Math.abs(-500);

// potencia

resultado = Math.pow(2,3);

// Minimo
resultado = Math.min(3,5,1,12,-3);

// Máximo 
resultado = Math.max(3,5,1,12,-3);

// Aleatorio
resultado = Math.random();

// Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 30);

console.log(resultado);